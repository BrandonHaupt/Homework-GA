const arr = [1,2,3,4,5,6]

// arr.forEach(ele => {
//     console.log(ele)A
// });

//proper way to write a .map function
// arr.map(function(item,index){return ele + 2})

// arr.map((ele) => {
//     console.log(ele + 2)
// })

//proper way to write a reduce function
// arr.reduce(function(acc, item, index){return acc + item}, 0)

//let reducedArr = arr.reduce((a,b) => {return a += b},0)